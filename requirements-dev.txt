# Development and CI tooling dependencies.
# Install alongside requirements.txt for local dev and in CI test jobs.
# Kept separate so pip-audit only audits production deps.
pytest>=9.0.2
pytest-cov>=7.1.0
ruff>=0.11.2
bandit>=1.9.0
pip-audit>=2.10.0
