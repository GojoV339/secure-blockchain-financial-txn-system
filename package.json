{
  "name": "block-chain-project-contracts",
  "version": "0.1.0",
  "description": "Hardhat Ethereum development environment for TransactionContract",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "hardhat": "^2.22.18",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "ethers": "^6.13.4"
  }
}
