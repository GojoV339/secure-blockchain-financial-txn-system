# Production / runtime dependencies only.
# Dev tools (pytest, ruff, bandit, pip-audit) live in requirements-dev.txt
# so that dependency audits never pull in their transitive deps (e.g. pygments).
cryptography>=44.0.2
flask>=3.1.0
pandas>=2.3.3
scikit-learn>=1.8.0
web3>=7.14.1
python-dotenv>=1.1.0